export enum FilterOptions {
  ByTags = 'tags',
  ByTitle = 'title',
}

export enum SortOptions {
  ByDate = 'date',
  ByViewCount = 'viewCount',
}

export enum SortDirections {
  Increase = 'increase',
  Decrease = 'decrease',
}
